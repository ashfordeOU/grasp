{
  "name": "grasp-github-action",
  "version": "3.20.0",
  "private": true,
  "license": "Elastic-2.0",
  "description": "GitHub Action for Grasp architecture analysis",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29",
    "@types/node": "^20",
    "@vercel/ncc": "^0.38.1",
    "jest": "^29",
    "ts-jest": "^29",
    "typescript": "^5"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.test.ts"
    ]
  }
}
